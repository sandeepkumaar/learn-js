## Array

- Search: O(n)
- Insert: O(n) + shift elements
- Delete: O(n) + shift elements

To avoid shifting elements - we use linkedlist

## Stack
Two ways 
- push, pop (end)
- unshift, shift (start)

## Queue
Two ways
- push, shift
- unshift, pop
